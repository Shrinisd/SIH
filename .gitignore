# PlatformIO / ESP-IDF build output
.pio
.pioenvs
.piolibdeps
build/

# VS Code local state (keep shared settings, drop machine-specific ones)
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
compile_commands.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS / editor cruft
.DS_Store
Thumbs.db

# Local search/debug dumps — never commit these
search_results.txt
*.local.txt


# ASR reference server output (local captures, never commit)
asr_captures/
# Regeneratable from dataset/raw via `python3 scripts/prepare_dataset.py`
# (fixed random seed → same split every time) -- not worth tracking, and it
# grows without bound since old runs' leftovers never get cleaned up
dataset/processed/
